spring:
  application:
    name: shipping-service
server:
  port: 8088
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always
